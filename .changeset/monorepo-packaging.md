---
"dropzone": patch
---

`CODE_OF_CONDUCT.md` is no longer part of the published package. It stays in the repository where GitHub looks for it, but the library now lives in `packages/dropzone` and npm can only pack files from inside that directory. Nothing else about the package changed.
